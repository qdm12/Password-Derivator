language: python
python:
  - "2.7"
  - "3.6"

install: "python install.py"

script:
  - nosetests --verbose --rednose --with-coverage --cover-erase --cover-package=server

after_success:
  - coveralls
